NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Só necessária para operações server-only que precisem ignorar RLS
# (ex.: admin criando operador sem passar pelo self-signup). Não expor
# com prefixo NEXT_PUBLIC_. Não usada pelo código ainda.
SUPABASE_SECRET_KEY=
